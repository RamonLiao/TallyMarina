# progress / session state
tasks/progress.md
tasks/lessons.md

# CLAUDE-related (per user global rule: 不上傳 git)
.claude/
CLAUDE.md
**/memory/
# ...but the triage-memory feature has a legit source module at this path; keep it tracked.
!services/api/src/triage/memory/
!services/api/src/triage/memory/**

# node / build
node_modules/
dist/
*.tsbuildinfo

# env / secrets
.env
.env.*
!.env.example

# move build artifacts
move/**/build/

# os
.DS_Store

# CLAUDE-authored review docs (per user global rule)
reviews/claude-review-*.md

# Project plan and notes (kept local)
plan.md
*-notes.md

# build/test artifacts (not source)
web/vite.config.js
web/vite.config.d.ts
.playwright-mcp/
desktop-1280.png
move/audit_anchor/Published.toml

# brainstorming visual-companion mockups (ephemeral)
.superpowers/brainstorm/

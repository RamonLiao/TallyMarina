[package]
name = "audit_anchor"
edition = "2024.beta"
license = "Apache-2.0"
# Fresh publish to Sui testnet 2026-07-03 (v1 chain orphaned: version field = struct layout change).
published-at = "0x6aa2a0404013d3ea7840ab510420b9a1cde98cd20ad8fb54e32b0bbc9e8eb12f"

[dependencies]
# Sui framework is bundled with the CLI (system dependency); no explicit git dep needed.

[addresses]
audit_anchor = "0x0"
